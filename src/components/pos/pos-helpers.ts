/** Small shared helpers for POS components (re-exports + aliases). */
export { formatMoney, formatDate, formatDateTime, useDebounced } from '@/lib/catalog/constants'
export { round2, computeLine, computeBill, splitGst } from '@/lib/pos/calc'
